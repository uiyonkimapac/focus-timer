# macOS
.DS_Store

# Playwright MCP test artifacts
.playwright-mcp/

# Verification screenshots (kept locally, not part of the app)
# (icons/*.png are real app assets — not ignored)
phase1-*.png
redesign-*.png
mobile-*.png
m3-*.jpeg

# Local backups of the single-file app (not for the repo)
index.backup.html
*.backup.html

# Internal planning notes (kept local — not for the public repo)
docs/COMMERCIALIZATION-ROADMAP.md
docs/RECOMMENDATIONS.md

# Test tooling (dev-only; the app itself stays a single static file)
node_modules/
test-results/
playwright-report/
/playwright/.cache/
