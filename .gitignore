# macOS
.DS_Store

# Playwright MCP test artifacts
.playwright-mcp/

# Verification screenshots (kept locally, not part of the app)
# (icons/*.png are real app assets — not ignored)
phase1-*.png
redesign-*.png
mobile-*.png
m3-*.jpeg

# Internal planning notes (kept local — not for the public repo)
docs/COMMERCIALIZATION-ROADMAP.md
docs/RECOMMENDATIONS.md
