{
  "name": "focus-timer",
  "version": "1.0.0",
  "private": true,
  "description": "Focus Timer — ADHD-first focus app (single-file PWA). The app itself is index.html with no build; this package.json only carries the dev test tooling.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.0"
  }
}
